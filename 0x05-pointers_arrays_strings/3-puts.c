#include "main.h"

/**
 * _puts - prints a string, followed by a new line, to stdout.
 * @s: The given string.
 *
 * Return: Void.
 */

void _puts(char *str)
{
	int i = 0;

	while (s[i] != '\0')
	{
		_putchar(s[i]);
	}

	_putchar('\n');
}
